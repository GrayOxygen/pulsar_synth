{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Standalone App",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/pulsar_synth_artefacts/Debug/Standalone/Pulsar Wang.app/Contents/MacOS/Pulsar Wang",
            "cwd": "${workspaceFolder}"
        },
        {
            "name": "Attach to DAW",
            "type": "lldb",
            "request": "attach",
            "pid": "${command:pickProcess}"
        }
    ]
}